{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "nuxt": "^3.1.2",
    "pug": "^3.0.2",
    "sass": "^1.58.0",
    "sass-loader": "^13.2.0"
  },
  "dependencies": {
    "date-fns": "^2.29.3"
  }
}
